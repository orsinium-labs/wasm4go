# https://taskfile.dev
version: '3'

tasks:
  build:
    - tinygo build -size short -target wasm-unknown -gc=leaking .
  run:
    - task: build
    - wasm2wat --generate-names ./tests.wasm
    - w4 run-native ./tests.wasm
